import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('AstroConnect', () => App);
AppRegistry.runApplication('AstroConnect', {
  rootTag: document.getElementById('root'),
});
